PORT=3000
NODE_ENV=development
JWT_SECRET=cambiar-este-secret-en-produccion
DB_PATH=./database.sqlite
CORS_ORIGIN=http://localhost:3000
# Email opcional (deja vacío para deshabilitar)
# En desarrollo (docker) usa:
# SMTP_HOST=mailhog
# SMTP_PORT=1025
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=contacto@ieee-unmsm.org
